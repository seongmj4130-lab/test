# min_feature_ic 최적화 비교 리포트

**생성일시**: 2025-01-XX  
**비교 기준**: min_feature_ic 파라미터 변경 전후

---

## 📋 변경 사항

### Config.yaml 수정

**수정 전**:
```yaml
l5:
  min_feature_ic: 0.005  # IC < 0.005인 피쳐 제거
```

**수정 후**:
```yaml
l5:
  min_feature_ic: 0.0  # IC > 0만 유지 (음수 IC 제거, IC 필터링 완화)
```

**의도**: 
- IC 필터링을 완화하여 더 많은 피처 사용
- 음수 IC 피처는 제거하고, IC > 0인 피처만 유지
- 피처 수 확대로 예측력 향상 기대

---

## 📊 Hit Ratio 비교

### 측정 결과

| 랭킹 유형 | min_feature_ic=0.005 | min_feature_ic=0.0 | 변화 | Dev | Holdout |
|-----------|----------------------|-------------------|------|-----|---------|
| **단기** | 41.58% | 41.58% | 0.00%p | 41.16% → 41.16% | 43.08% → 43.08% |
| **장기** | 38.72% | 38.72% | 0.00%p | 38.13% → 38.13% | 41.45% → 41.45% |
| **통합** | 41.58% | 41.58% | 0.00%p | 41.16% → 41.16% | 43.08% → 43.08% |

### 분석

**결과**: Hit Ratio 변화 없음 (0.00%p)

**가능한 원인**:

1. **L5 모델 학습 미실행**
   - `min_feature_ic`는 L5 모델 학습 단계에서만 적용됨
   - 랭킹 모델(L8)은 L5 모델을 사용하지 않음
   - 랭킹 모델만 실행하면 L5 파라미터 변경이 반영되지 않음

2. **피처 필터링 효과 제한적**
   - IC > 0.005인 피처와 IC > 0인 피처가 거의 동일할 수 있음
   - 실제로 제거되는 피처가 적을 수 있음

3. **랭킹 모델의 독립성**
   - 랭킹 모델은 L5 모델 예측을 사용하지 않고 직접 피처로 랭킹 생성
   - L5 파라미터 변경이 랭킹 모델 Hit Ratio에 영향을 주지 않음

---

## ⚠️ 중요 발견

### L5 vs L8 파이프라인 분리

**L5 모델 학습 파이프라인**:
- `min_feature_ic` 적용됨
- Ridge 모델 학습
- 예측값 생성 (`y_pred`)

**L8 랭킹 엔진 파이프라인**:
- `min_feature_ic` 적용 안 됨
- 직접 피처로 랭킹 생성
- L5 모델 예측 사용 안 함

**결론**: 
- 랭킹 모델만 실행하면 L5 파라미터 변경이 반영되지 않음
- `min_feature_ic` 변경 효과를 확인하려면 **L5 모델 학습부터 재실행** 필요

---

## 🔍 현재 적용 중인 파라미터

### L5 모델 학습 (랭킹 모델에 미사용)

- `ridge_alpha`: 1.5
- `min_feature_ic`: **0.0** (수정됨)
- `filter_features_by_ic`: True
- `use_rank_ic`: True

### L8 랭킹 엔진 (실제 사용 중)

- `feature_weights_config_short`: `configs/feature_weights_short_hitratio_optimized.yaml`
- `feature_weights_config_long`: `configs/feature_weights_long_ic_optimized.yaml`
- 피처별 가중치 적용 중

---

## 📝 결론

### 현재 상태

1. ✅ **Config 수정 완료**: `min_feature_ic: 0.0`으로 변경
2. ⚠️ **랭킹 모델에 미반영**: L5 파라미터는 랭킹 모델에 사용되지 않음
3. ✅ **Hit Ratio 동일**: 랭킹 모델만 실행하여 변화 없음

### 해석

- `min_feature_ic`는 L5 모델 학습에만 적용되는 파라미터
- 랭킹 모델은 L5 모델을 사용하지 않으므로 영향 없음
- 랭킹 모델 Hit Ratio 개선을 위해서는 L8 파라미터 최적화 필요

### 권장 사항

1. **L5 모델 학습 재실행** (min_feature_ic 효과 확인용)
   - L5 모델 학습부터 재실행하여 피처 필터링 효과 확인
   - L5 모델 예측 기반 백테스트로 Hit Ratio 측정

2. **L8 랭킹 엔진 최적화** (랭킹 모델 Hit Ratio 개선용)
   - 피처 가중치 튜닝
   - 피처 선택 최적화
   - 정규화 방식 변경

---

**비교 파일**:
- 수정 전: `artifacts/reports/ranking_hit_ratio_with_feature_weights_final.csv`
- 수정 후: `artifacts/reports/ranking_hit_ratio_min_ic_0.0.csv`

**참고**: 랭킹 모델은 L5 모델을 사용하지 않으므로, `min_feature_ic` 변경이 랭킹 모델 Hit Ratio에 영향을 주지 않습니다.

