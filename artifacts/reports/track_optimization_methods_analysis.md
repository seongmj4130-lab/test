# 트랙 A vs 트랙 B 최적화 방식 분석

**작성일**: 2026-01-07
**목적**: 랭킹 엔진 피처 가중치(트랙 A)와 백테스트 파라미터(트랙 B)의 최적화 방식 비교 분석

---

## 📊 개요

프로젝트는 두 개의 독립적인 트랙으로 구성되어 있으며, 각각 다른 목표와 방법으로 최적화됩니다:

1. **트랙 A**: 랭킹 엔진의 피처 가중치 최적화
2. **트랙 B**: 투자 모델/백테스트의 top_k, weighting 등 파라미터 최적화

---

## 🔍 트랙 A: 랭킹 엔진 피처 가중치 최적화

### 목표
랭킹 엔진(L8)에서 사용하는 피처 가중치를 최적화하여 **랭킹의 예측력**을 향상시킵니다.

### 최적화 대상
- **피처별 가중치**: 각 피처에 할당되는 가중치 값
- **그룹 가중치**: value, profitability, technical, news 등 그룹별 가중치

### 최적화 방법

#### 1. **단기 랭킹 (Hit Ratio 기반 최적화)**

**최적화 목표**: Holdout Hit Ratio 50% 이상 달성

**파일**: `configs/feature_weights_short_hitratio_optimized.yaml`

**최적화 프로세스**:
1. **현재 상태 분석**
   - Dev Hit Ratio: 57.32%
   - Holdout Hit Ratio: 43.48%
   - 과적합: 13.84% (Dev - Holdout)

2. **가중치 재조정**
   ```yaml
   # 변경 전
   technical: 0.60  # 과적합 유발 가능성 높음
   value: 0.15
   profitability: 0.10

   # 변경 후
   technical: 0.50  # 과적합 유발 피쳐 축소
   value: 0.20      # 재무 지표 강화
   profitability: 0.15  # 수익성 지표 강화
   ```

3. **피처별 세부 가중치 조정**
   - Core 공통 피처: 0.021 (약간 감소)
   - Short 전용 피처: 0.025 (증가)
   - News 감성: 0.10 (가중치 증가)

**최적화 스크립트**: `scripts/optimize_hit_ratio_bt20_short.py`

**결과**:
- 피처 22개 사용 (OHLCV 제거)
- News 가중치: 0.10으로 증가
- 단기 전용 피처 가중치: 0.025로 증가

#### 2. **장기 랭킹 (IC 기반 최적화)**

**최적화 목표**: IC (Information Coefficient) 최대화

**파일**: `configs/feature_weights_long_ic_optimized.yaml`

**최적화 프로세스**:
1. **IC 계산**: 각 피처의 IC 값 계산
2. **가중치 할당**: IC가 높은 피처에 더 높은 가중치
3. **피처 선택**: IC가 낮은 피처 제거 또는 가중치 감소

**결과**:
- 피처 19개 사용 (OHLCV 제거)
- ESG 포함 (environmental_score, social_score, governance_score)
- News 가중치: 0.03 (단기보다 낮음)
- Long 전용 피처 가중치: 0.052

### 최적화 방식 요약

| 항목 | 단기 랭킹 | 장기 랭킹 |
|------|----------|----------|
| **최적화 목표** | Hit Ratio ≥ 50% | IC 최대화 |
| **최적화 지표** | Holdout Hit Ratio | Information Coefficient |
| **방법** | 수동 가중치 조정 + 과적합 분석 | IC 기반 자동 가중치 |
| **피처 수** | 22개 | 19개 |
| **News 가중치** | 0.10 | 0.03 |
| **최적화 스크립트** | `optimize_hit_ratio_bt20_short.py` | IC 계산 스크립트 |

### 특징

1. **수동 최적화**:
   - Grid Search 없음
   - 도메인 지식 기반 수동 조정
   - 과적합 분석 후 가중치 재조정

2. **목표 지표별 최적화**:
   - 단기: Hit Ratio (분류 정확도)
   - 장기: IC (연속형 예측력)

3. **피처 그룹 기반 접근**:
   - value, profitability, technical, news 그룹별 가중치 관리
   - 그룹 내 피처는 균등 분배 또는 수동 지정

---

## 🎯 트랙 B: 백테스트 파라미터 최적화

### 목표
백테스트(L7)에서 사용하는 파라미터를 최적화하여 **백테스트 성과(Sharpe, CAGR, MDD 등)**를 향상시킵니다.

### 최적화 대상
- `top_k`: 상위 선택 종목 수
- `buffer_k`: 버퍼 영역 종목 수
- `weighting`: 가중치 방식 (equal, softmax)
- `softmax_temperature`: Softmax 가중치 온도
- `regime` 파라미터: 시장 국면별 top_k, exposure
- `risk_scaling` 파라미터: 리스크 스케일링 계수

### 최적화 방법

#### 1. **Grid Search 기반 최적화**

**최적화 스크립트**:
- `scripts/grid_search_optimization.py` (전략별 그리드 서치)
- `scripts/optimize_bt20_regime_grid.py` (Regime 파라미터 그리드 서치)

**Grid 정의** (BT20 예시):
```python
BT20_GRID = {
    "top_k": [10, 12, 15, 18],
    "buffer_k": [12, 15, 18],
    "weighting": ["equal", "softmax"],
    "softmax_temperature": [0.3, 0.5, 0.7],
    "regime_exposure_bull_strong": [1.3, 1.5, 1.7],
    "regime_exposure_bull_weak": [1.0, 1.2, 1.4],
    "volatility_adjustment_min": [0.6, 0.7],
}
```

**최적화 프로세스**:
1. **파라미터 그리드 생성**: 모든 조합 생성
2. **병렬 백테스트 실행**: 각 조합별 백테스트 수행
3. **목적함수 계산**: Holdout 구간 기준으로 최적화 점수 계산
   ```python
   objective_score = (2.0 * sharpe) + (0.5 * (-mdd)) - (1.0 * turnover) - (20.0 * cost)
   ```
4. **최적 조합 선택**: 목적함수가 최대인 조합 선택

**최적화 목표별 전략**:
- **BT20 (단기)**: CAGR 중심 최적화
- **BT120 (장기)**: Calmar Ratio 중심 최적화

#### 2. **Top K 최적화**

**최적화 리포트**: `artifacts/reports/full_replacement_topk_optimization_report.md`

**실험 조건**:
- 완전 교체 전략 (rebalance_interval = holding_days)
- top_k 그리드: [20, 15, 10]
- buffer_k=0 고정

**결과**:
- 최적 top_k: **15**
- Holdout Total Return: 12.39%
- Holdout Sharpe: 0.5464

#### 3. **Weighting 방식 최적화**

**최적화 리포트**: `artifacts/reports/weighting_comparison_optimization_report.md`

**비교 대상**:
- `equal`: 균등 가중치
- `softmax`: Softmax 가중치 (temperature 파라미터 필요)

**결과**:
- `equal`이 대부분의 경우 더 우수한 성과
- `softmax`는 일부 전략에서 약간의 개선

#### 4. **Regime 파라미터 최적화**

**최적화 스크립트**: `scripts/optimize_bt20_regime_grid.py`

**Grid 정의**:
```python
neutral_band_grid = [0.0, 0.02, 0.05]
top_k_bear_grid = [20, 30]
exposure_bear_grid = [0.7, 0.8, 1.0]
risk_bear_mult_grid = [0.7, 0.8, 0.9]
```

**목적함수**:
```python
def _objective(m_hold: Dict) -> float:
    s = float(m_hold.get("net_sharpe", 0.0) or 0.0)
    mdd = float(m_hold.get("net_mdd", 0.0) or 0.0)  # 음수
    to = float(m_hold.get("avg_turnover_oneway", 0.0) or 0.0)
    cost = float(m_hold.get("avg_cost_pct", 0.0) or 0.0)
    return (2.0 * s) + (0.5 * (mdd * -1.0)) - (1.0 * to) - (20.0 * cost)
```

**결과**:
- 최적 neutral_band: 0.0
- 최적 top_k_bear: 20
- 최적 exposure_bear: 0.6
- 최적 risk_bear_multiplier: 0.8

#### 5. **Ridge Alpha 최적화 (L5 모델 파라미터)**

**최적화 리포트**: `artifacts/reports/FINAL_RIDGE_ALPHA_OPTIMIZATION_REPORT.md`

**Grid**: [0.01, 0.1, 0.3, 0.5, 1.0, 3.0, 5.0, 10.0]

**중요 발견**:
⚠️ **랭킹 기반 전략에서는 ridge_alpha가 성과에 영향을 주지 않음**
- 모든 전략이 `score_col: score_total_short/long/ens` (랭킹 기반) 사용
- L5 모델 예측값이 아닌 랭킹 점수 사용
- 따라서 Ridge Alpha 최적화는 의미 없음

### 최적화 방식 요약

| 항목 | 내용 |
|------|------|
| **최적화 방법** | Grid Search (자동화) |
| **최적화 목표** | Holdout 구간 성과 최대화 |
| **목적함수** | Sharpe, CAGR, MDD, Turnover, Cost 조합 |
| **실행 방식** | 병렬 백테스트 실행 |
| **최적화 스크립트** | `grid_search_optimization.py`, `optimize_bt20_regime_grid.py` |
| **결과 저장** | CSV, Parquet, Markdown 리포트 |

### 특징

1. **자동화된 Grid Search**:
   - 모든 파라미터 조합 자동 생성
   - 병렬 처리로 빠른 실행

2. **목적함수 기반 최적화**:
   - Sharpe, CAGR, MDD, Turnover, Cost를 가중합으로 결합
   - Holdout 구간 기준으로 평가

3. **전략별 최적화**:
   - BT20: CAGR 중심
   - BT120: Calmar Ratio 중심

4. **파라미터 독립성**:
   - L6R (랭킹 스코어 변환) 결과를 재사용
   - L7만 재실행하여 빠른 최적화

---

## 🔄 트랙 A vs 트랙 B 비교

### 최적화 방식 비교

| 항목 | 트랙 A (랭킹 엔진) | 트랙 B (백테스트) |
|------|------------------|------------------|
| **최적화 대상** | 피처 가중치 | top_k, weighting, regime 등 |
| **최적화 방법** | 수동 조정 + 도메인 지식 | Grid Search (자동화) |
| **최적화 지표** | Hit Ratio (단기), IC (장기) | Sharpe, CAGR, MDD 조합 |
| **실행 빈도** | 드물음 (수동 조정) | 자주 (자동화) |
| **의존성** | 독립적 (L8만 영향) | L6R 결과 필요 |
| **파일 위치** | `configs/feature_weights_*.yaml` | `configs/config.yaml` (l7_* 섹션) |

### 최적화 흐름도

```
┌─────────────────────────────────────────────────────────────┐
│                    트랙 A: 랭킹 엔진                         │
│                                                             │
│  피처 가중치 (feature_weights_*.yaml)                       │
│         ↓                                                   │
│  L8: 랭킹 생성                                              │
│         ↓                                                   │
│  Hit Ratio / IC 계산                                        │
│         ↓                                                   │
│  가중치 수동 조정 (과적합 분석 기반)                         │
│         ↓                                                   │
│  ranking_short_daily.parquet                                │
│  ranking_long_daily.parquet                                 │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    트랙 B: 백테스트                          │
│                                                             │
│  L6R: 리밸런싱 스코어 변환                                  │
│         ↓                                                   │
│  L7: 백테스트 (Grid Search)                                 │
│         ↓                                                   │
│  파라미터 조합별 성과 평가                                  │
│         ↓                                                   │
│  최적 파라미터 선택 (목적함수 기반)                         │
│         ↓                                                   │
│  config.yaml 업데이트 (l7_* 섹션)                          │
└─────────────────────────────────────────────────────────────┘
```

### 독립성

**트랙 A와 트랙 B는 독립적으로 최적화됩니다:**

1. **트랙 A 최적화**:
   - 피처 가중치만 변경
   - 랭킹 점수 (`score_total_short/long`) 생성
   - 트랙 B 설정은 영향 없음

2. **트랙 B 최적화**:
   - 랭킹 점수를 입력으로 사용 (L6R 단계)
   - 백테스트 파라미터만 변경
   - 트랙 A 설정은 영향 없음

3. **최적화 순서**:
   - 일반적으로 **트랙 A → 트랙 B** 순서
   - 트랙 A 최적화 완료 후 트랙 B 최적화 진행
   - 트랙 B 최적화는 L6R 결과 재사용 가능

---

## 📝 최적화 결과 요약

### 트랙 A 최적화 결과

**단기 랭킹** (`feature_weights_short_hitratio_optimized.yaml`):
- 피처 22개
- News 가중치: 0.10
- 단기 전용 피처: 0.025
- 목표: Holdout Hit Ratio ≥ 50%

**장기 랭킹** (`feature_weights_long_ic_optimized.yaml`):
- 피처 19개
- ESG 포함
- News 가중치: 0.03
- Long 전용 피처: 0.052
- 목표: IC 최대화

### 트랙 B 최적화 결과

**최적 파라미터** (06_code22 기준):

| 전략 | top_k | buffer_k | weighting | rebalance_interval |
|------|-------|----------|-----------|-------------------|
| bt20_short | 12 | 15 | equal | 20 |
| bt20_ens | 15 | 20 | softmax | 20 |
| bt120_long | 15 | 15 | equal | 20 |
| bt120_ens | 20 | 15 | equal | 20 |

**최적 Regime 파라미터** (bt20_ens):
- neutral_band: 0.0
- top_k_bear: 20
- exposure_bear: 0.6
- risk_bear_multiplier: 0.8

---

## 💡 권장사항

### 트랙 A 최적화
1. **과적합 분석 필수**: Dev vs Holdout Hit Ratio 차이 모니터링
2. **도메인 지식 활용**: 피처 그룹별 가중치 조정 시 비즈니스 로직 고려
3. **IC 계산 자동화**: 장기 랭킹 최적화를 위한 IC 계산 스크립트 활용

### 트랙 B 최적화
1. **Grid Search 활용**: 자동화된 최적화로 효율성 향상
2. **목적함수 조정**: 전략별 목표에 맞게 목적함수 가중치 조정
3. **Holdout 우선**: Dev 구간 과적합 방지를 위해 Holdout 기준 최적화

### 통합 최적화
1. **순차적 접근**: 트랙 A 완료 후 트랙 B 진행
2. **결과 모니터링**: 각 트랙 최적화 후 전체 파이프라인 재실행하여 최종 성과 확인
3. **독립성 유지**: 트랙 간 설정 파일 분리로 독립성 보장

---

**작성자**: Cursor AI
**최종 업데이트**: 2026-01-07
